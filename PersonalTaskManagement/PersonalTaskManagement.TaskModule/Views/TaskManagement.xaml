<UserControl x:Class="PersonalTaskManagement.TaskModule.TaskManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Label Content="任务管理" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,0,0,0" />
        <ListBox Grid.Row="1" Margin="4" />
        <Button Content="添加" HorizontalAlignment="Right" Margin="4" Width="50" />
    </Grid>
</UserControl>